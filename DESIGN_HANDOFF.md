# 디자인 핸드오프 — 선거 후보 비교 웹앱

> **읽는 사람**: Claude Design (디자인 담당)
> **목적**: 이 앱의 **모든 화면과 그 안의 모든 컴포넌트, 각자의 역할**을 빠짐없이 전달하여, 정해진 방향에 얽매이지 않고 **자유롭게 5가지 시안을 발산**할 수 있게 한다.
> **중요**: 아래 "현재 구현"·"기존 토큰"은 **맥락 참고용일 뿐, 따라야 할 정답이 아니다.** 색·타이포·레이아웃·모션·정보 시각화 방식은 전부 새로 제안해도 된다. 반드시 지켜야 하는 것은 [§9 하드 제약](#9-반드시-지킬-제약-hard-rules)뿐이다.

---

## 1. 제품 개요

대한민국 지방선거(제9회 전국동시지방선거, 2026) **후보자 비교 웹앱**이다. 한 선거구의 후보들을 **나란히(side-by-side)** 놓고, 유권자가 직접 비교·판단하도록 돕는다.

- **다루는 내용**: 후보의 인적사항·경력·재산·납세·병역·전과(중앙선관위 공개자료) + 5대 공약 + 선거공보 세부 공약 + **공약 적정성 교차검증**(타 정당 균형패널 평가) + **블라인드 공약 퀴즈**.
- **핵심 가치**: ① 공정·중립(특정 후보/정당 편들지 않음) ② 근거·출처 투명성 ③ 정보를 쉽게 견주어 보는 비교 가독성.
- **톤 후보군(자유 선택)**: 신뢰감 있는 공공·저널리즘 톤도, 친근한 시민 참여 톤도, 데이터 중심 분석 톤도 모두 가능하다. 톤 자체를 시안마다 달리 제안해도 좋다.
- **현재 다루는 선거구(예시 데이터)**: 「용인특례시장」(후보 3명), 「경기도지사」(후보 5명). 선거구는 더 늘어날 수 있다 → **후보 N명(3~5명, 그 이상도)을 가로로 비교**하는 상황을 항상 전제할 것.

---

## 2. 기술·산출물 컨텍스트

- 구현 스택: **React + TypeScript + SCSS(CSS Modules)**. (Tailwind 미사용.) — 디자인 산출물이 곧바로 이 구조로 옮겨질 수 있게, **컴포넌트 단위로 정리된 시안 + 토큰(색/타이포/간격/radius/그림자/모션)** 형태면 가장 좋다.
- **클래스 네이밍**: 의미 있는 요소엔 PascalCase className(예: `CandidateSummaryCard`, `VerdictChip`). 디자인 시 컴포넌트/요소 이름을 함께 제시하면 핸드오프가 매끄럽다.
- 배포: 정적 사이트(GitHub Pages). 외부 디자인 자산(폰트 등)은 가벼운 것이 좋다.
- **다국어 없음(한국어 전용)**. 단, 한국어 문장이 **길다**(아래 §7 카피 샘플 참고) — 줄바꿈·말줄임·박스 넘침을 디자인 단계에서 반드시 고려.

---

## 3. 정보 구조 (화면 지도)

앱은 두 개의 최상위 모드로 나뉜다.

```
앱 (공개)                                   관리자 콘솔 (운영자 전용 · 별도 진입)
├─ [공통 셸]                                 └─ AdminConsole
│   ├─ AppHeader  (타이틀 + 선거구 선택 + 콘솔 진입)
│   ├─ TabBar     (4개 탭 전환)
│   └─ AppFooter  (출처·안내)
│
├─ 탭1. 종합 비교   (Overview)        ← 기본 진입 = "메인 화면"
├─ 탭2. 공약 비교   (Pledges)
├─ 탭3. 인물·검증   (Verification)
├─ 탭4. 공약 퀴즈   (Quiz)
│
└─ [오버레이] CandidateDetailModal  (후보 1인 전체 정보)
```

- **"메인 화면" 정의**: 앱에 처음 들어왔을 때 보이는 화면 = **AppHeader + TabBar + 탭1(종합 비교)**. 5개 시안은 이 메인 화면을 우선 대상으로 한다([§10](#10-작업-지침-5가지-시안-메인-화면-우선)).
- 탭 전환은 같은 셸(헤더/탭바/푸터) 안에서 본문(`main`)만 바뀐다.
- 관리자 콘솔은 **운영자 전용 비공개 화면**이라 디자인 우선순위가 낮다(시안 대상 아님, 마지막 고도화 단계에서만 가볍게).

---

## 4. 화면별 상세

각 화면에 대해 **목적 / 상태·진입 / 구성 컴포넌트(역할·핵심 데이터)** 를 기술한다.

### 4.0 공통 셸 (모든 탭에 공유)

| 컴포넌트 | 역할 | 핵심 데이터 / 상태 |
|---|---|---|
| **AppHeader** | 상단 타이틀 영역. 선거 제목·부제·안내문 표시. 선거구가 2개 이상이면 **선거구 선택 드롭다운**. 우측에 **관리자 콘솔 진입 버튼**. | `meta.title/subtitle/note`, 선거구 목록, 선택값 |
| **TabBar** | 4개 섹션 탭 내비게이션(종합 비교 / 공약 비교 / 인물·검증 / 공약 퀴즈). 현재 탭 강조. | `tabs[]`, `activeKey` |
| **AppFooter** | 출처(중앙선관위 등)·면책·안내 문구. | `meta.source/note` |
| **SectionTitle** | 본문 안 섹션 제목 + 보조 설명문. 여러 화면이 공유. | `title`, `description?`(길 수 있음) |

> 디자인 포인트: 헤더의 **선거구 선택기**는 "지금 보는 선거가 무엇인지"를 분명히 해야 한다(맥락 전환). 탭바는 4개로 고정.

---

### 4.1 탭1 — 종합 비교 (Overview) · **메인 화면**

**목적**: 첫인상. 후보들을 한눈에 파악하고 비교를 시작하게 한다.

| 컴포넌트 | 역할 | 핵심 데이터 |
|---|---|---|
| **CandidateSummaryCard** ×N | 후보별 요약 카드. 기호 배지·이름·정당·슬로건/한 줄 비전, 그리고 공약 적정성 요약(적정/주의/부적정 개수). "상세 정보 보기" → 모달 오픈. | 후보 기본정보 + `pledgeReviews` 집계 |
| **CompareGrid** (quickRows) | 후보(열) × 핵심 항목(행) 비교표. 행 = 나이·성별 / 직업 / 학력 / 한 줄 비전. | 후보별 단답·문장 |

- 후보 수만큼 카드가 가로로 늘어선다(3~5장+). 카드 그리드의 **반응형 재배치**가 중요.
- `CandidateSummaryCard`는 이 앱의 "얼굴". 시안 차별화의 핵심이 되는 컴포넌트.

---

### 4.2 탭2 — 공약 비교 (Pledges)

**목적**: 분야별로 후보들의 공약을 정밀 대조한다.

| 컴포넌트 | 역할 | 핵심 데이터 / 인터랙션 |
|---|---|---|
| **IntegratedPledgeTable** | **분야(행) × 후보(열) 통합 표.** 한 셀 안에 그 후보의 해당 분야 공약들이 **칩(chip)** 으로 들어간다. | 8개 분야 × 후보 N명 |
| └ 공약 칩 (PledgeChip) | "공약 N" + 성격(공약/목표) + 적정성 표시 + 미니 배지(연속/기추진/재탕) + 제목 요약. | 5대 공약 1건 |
| └ 공보 칩 (BulletinChip) | 선거공보 세부 공약 묶음(분야별 N건). | 공보 공약 그룹 |
| └ **상세 툴팁**(호버/클릭 고정) | 칩에 호버하면 **목표·이행방법·이행기간·재원조달** + 적정성 평가(아래 PledgeReviewDetail)를 띄움. 클릭하면 고정(pin). | 공약 전문 + 검증 |

- 표는 가로로 넓어질 수 있어 **가로 스크롤 + 분야 라벨 고정(sticky)** 등 큰 표의 가독성 설계가 핵심.
- 모바일에서 "분야×후보 매트릭스"를 어떻게 풀지는 자유(아코디언, 분야별 카드, 후보 토글 등).
- 툴팁은 화면 밖으로 안 잘리게(뷰포트 경계 처리) — 패턴은 자유.

> 참고: 과거엔 "분야별/후보별" 토글 두 화면이었으나 **하나의 통합 표로 합쳤다.** (구버전 컴포넌트 `CategoryCompareView`, `PledgeBoard`는 잔존 코드이며, `PledgeCard`/`BulletinPolicies`는 현재 **모달**에서 재사용된다.)

---

### 4.3 탭3 — 인물·검증 (Verification)

**목적**: 후보의 자격·도덕성 지표를 항목별로 나란히 검증한다. 근거·출처를 분명히.

| 컴포넌트 | 역할 | 핵심 데이터 |
|---|---|---|
| **CompareGrid** (verificationRows) | 후보(열) × 검증 항목(행). | 아래 행들 |
| └ 행: 학력 / 주요 경력 | 텍스트·리스트 | |
| └ 행: **선관위 자료 제출** → **MaterialStatus** | 선거공보·선거공약서·5대공약을 **성실히 제출했는지** 체크(미제출 강조). | 제출 여부 3종 |
| └ 행: 재산(신고 총액) | 금액(채무초과 시 음수·강조). | 천원 단위 |
| └ 행: 납세(최근 5년) | 금액. | |
| └ 행: **재산 대비 실질 납세율** | (납세액 − 현 체납액) ÷ 재산 × 100. 비율 강조 + 산식 보조표기. | 계산값(%) |
| └ 행: 체납 → **StatusChip** | 현 체납액 유무를 색 칩으로(있음=위험/없음=양호). | |
| └ 행: 병역 | 본인+직계 병역. | |
| └ 행: 전과 → **StatusChip** + 리스트 | 전과 건수·내역. | |
| **PledgeReviewPanel** | 화면 하단. **공약 적정성 교차검증** 결과. 후보별 카드로, 검증 패널(어느 정당들이 평가)·출처·적정/주의/부적정 집계 + 5대 공약 항목별 상세. | `pledgeReviews` |
| └ **PledgeReviewDetail**(공유) | 한 공약의 ① **연속성·재활용 점검 박스**(연속=긍정 / 기추진·재탕=주의, 각각 근거) ② 실현가능성·구체성 등급+근거 ③ 종합 사유 ④ 균형패널 표결. | 공약 1건 평가 |
| └ **RecycleMiniBadges**(공유) | 연속/기추진/재탕을 색·텍스트 배지로(이모지 없이). | |

- 이 화면은 **"근거·등급 표시"가 핵심**이다. 등급(상/중/하, 적정/주의/부적정)을 **텍스트 옆 근거 문장과 시각적으로 명확히 분리**해야 한다(과거 "중 — 긴 설명" 인라인 표기가 가독성 나빴음 → 등급은 배지/뱃지, 근거는 본문으로 분리). 이 분리를 어떻게 우아하게 풀지가 디자인 과제.
- 3색 의미 체계가 반복 등장: **양호/적정(긍정)·주의(중간)·위험/부적정(부정)**. 일관된 색·기호 언어 설계 필요(단, 색만으로 구분 금지 — §9).

---

### 4.4 탭4 — 공약 퀴즈 (Quiz)

**목적**: 후보명을 가린 채 정책·검증 항목을 고르게 해, "내 생각과 가장 가까운 후보"를 알려주는 **참여형 게임**. (이 화면만 톤이 더 가벼워도 된다.)

| 컴포넌트 | 역할 | 상태 |
|---|---|---|
| **QuizView** | 퀴즈 전체 컨테이너. 단계 진행·이전 이동·후보 배제 관리. | 단계 인덱스, 응답 누적 |
| **QuizIntro** | 시작 화면. 닉네임 입력 + 최근 결과 미리보기. | 닉네임, 로컬 기록 |
| **QuizQuestion** | 정책 블라인드 문항. 후보를 가린 선택지(2~3개) 중 **1~2개 선택**. 진행률 표시. | 진행률, 선택 |
| **QuizVerifyQuestion** | 검증 문항. 후보의 병역·체납·전과·부실공약을 보고 **수용 정도 3단계**로 답함(후보명 비공개). | 진행률, 선택 |
| **QuizResult** | 결과. 추천/배제 후보 + 후보별 점수(막대) + 내 정책·검증 응답 공개. | 점수·정답 매핑 |
| **QuizHistory** | 브라우저에 저장된 이전 결과 목록. | 로컬 저장값 |

- 문항 → 문항 → 결과로 이어지는 **흐름·전환·진행감(progress)** 설계가 중요.
- 결과 화면의 **점수 시각화**(막대/랭킹/매칭율)는 창의 발휘 지점.
- 이 탭은 **이모지 사용 허용**(게임성). 다른 탭은 §9 이모지 정책을 따름.

---

### 4.5 오버레이 — 후보 상세 모달 (CandidateDetailModal)

**목적**: 한 후보의 **모든 정보**를 한 곳에서. 종합 비교 카드의 "상세 보기"로 열림.

| 구성 | 역할 |
|---|---|
| 헤더: 기호 배지·이름·정당·슬로건 / 닫기 | |
| **InfoItem** 들 | 생년월일·나이·성별·직업·학력·경력·재산·납세·병역·전과 라벨-값 쌍. |
| **PledgeCard** ×5 | 5대 공약을 펼침/접힘 카드로. 목표·이행방법·이행기간·재원조달(**PledgeSection**) + 적정성 평가(PledgeReviewDetail). |
| **BulletinPolicies** | 선거공보 분야별 세부 공약(펼침/접힘 그룹). |

- 모달의 **레이아웃·스크롤·닫기 인터랙션**, 그리고 카드 펼침/접힘 패턴이 과제.

---

### 4.6 관리자 콘솔 (AdminConsole) — *비공개, 우선순위 낮음*

운영자가 선관위 자료 링크 → 선거구명 입력 → 후보 PDF 일괄 업로드 → 자동 분석. **시안 대상 아님.** 최종 고도화 단계에서 공개 앱과 톤만 맞추면 됨.

---

## 5. 공용 컴포넌트 카탈로그 (여러 화면 공유)

| 컴포넌트 | 쓰임 | 비고 |
|---|---|---|
| **CandidateBadge** | 기호 번호를 **정당 색 원형 배지**로. 거의 모든 화면. | 크기 sm/md. 후보 식별의 기본 단위 |
| **StatusChip** | 양호/주의/위험 상태 칩(전과·체납 등). | 색 + **반드시 텍스트 병기** |
| **CompareGrid** | 범용 비교표(열=후보, 행=항목, 셀=커스텀 렌더). | 종합·검증 공유. 큰 표 가독성의 기반 |
| **SectionTitle** | 섹션 제목 + 설명. | 설명문이 길 수 있음 |
| **PledgeReviewDetail** / **RecycleMiniBadges** | 공약 적정성 상세·배지. | 공약비교 툴팁 + 인물검증 패널 + 모달에서 공유 |
| **PledgeCard** / **PledgeSection** / **BulletinPolicies** | 공약 카드·섹션·공보 그룹. | 현재 모달에서 사용 |

> **반복 등장하는 "비교 단위"**: ① 후보 식별(배지+이름+정당색) ② 등급·상태(3색 의미 체계) ③ 펼침/접힘(공약·공보) ④ 호버 툴팁(공약 상세). 이 4가지의 시각 언어를 일관되게 잡는 것이 디자인 시스템의 뼈대.

---

## 6. 데이터 모델 (디자인에 영향을 주는 형태·범위)

디자인이 수용해야 할 데이터의 **모양과 양**:

- **후보(Candidate)**: 기호(1~), 이름, 정당, **정당색(HEX)**, 슬로건, 한 줄 비전, 생년월일/나이/성별/직업, 학력(한 줄), 경력(여러 줄 리스트), 포스터 이미지(선거공보 표지, 세로 비율 ≈ 19:27), 5대 공약, 전과/납세/병역/재산, 자료 제출 여부.
  - **수**: 한 선거구에 **3~5명**(가변, 더 많을 수도). 가로 비교가 늘 전제.
- **공약(Pledge)** ×5: rank(1~5), 제목, **분야(8종)**, 목표/이행방법/이행기간/재원(각각 여러 줄), **성격**(공약=임기 내 이행 / 목표=장기 지향).
  - 8개 분야: 교통 · 반도체·미래산업 · 경제·일자리 · 복지·생활 · 도시·개발 · 교육 · 문화·관광 · 주거·환경.
- **선거공보 세부 공약**: 분야별 묶음(headline + 항목 리스트, 분야당 수 건).
- **공약 적정성 평가(PledgeReviewItem)**: 실현가능성(상/중/하)+근거, 구체성(구체적/보통/모호)+근거, **연속**(긍정)·**기추진**·**재탕**(주의) 각 근거, **종합 판정(적정/주의/부적정)**, 균형패널 표결 문자열.
- **금액**: 모두 천원/원 단위 숫자 → **고정폭(mono) 표기** 권장(자릿수 정렬). 채무초과(음수) 케이스 존재.
- **상태 3색 의미**: 긍정(전과없음/체납없음/적정/연속) · 주의 · 부정(전과/체납/부적정).

엣지 케이스(디자인에 꼭 반영):
- 어떤 후보는 특정 분야 **공약 없음**(빈 셀) → "—" 등 빈 상태 표현 필요.
- 평가 데이터가 **없는 선거구**도 가능(적정성 패널·퀴즈 검증 문항이 통째로 생략됨).
- 경력·전과 리스트가 **0~여러 줄**로 가변.
- 근거 문장이 **2~3줄로 길다**.

---

## 7. 콘텐츠·카피 샘플 (실제 길이감)

디자인 시 실제 한국어 문장 길이를 가늠하도록, 현재 들어가는 텍스트 예시:

- 슬로건: `"당당한 경기! 든든한 추미애"` / 한 줄 비전: `"반도체로 키우고 교통으로 잇는 미래 도시"`
- 공약 제목(길다): `"AI·반도체 중심 대전환으로 도민 1인당 GRDP 1억원 시대"`, `"빠르고 편리한 Door-to-Door 교통체계 구축"`
- 등급+근거(분리 표기 대상): 등급 `중` / 근거 `"GTX-D~H 국가철도망 반영은 국토부 소관, 도지사 직접 결정 불가"`
- 검증 근거(길다): `"판단근거(공통 출처): 1기 신도시 재정비·공공임대 확대는 국토부 선도지구 지정(2024.11)에서 비롯된 의제로 같은 선거 다수 후보가 공통 제시 — 동시 출마자라 후보 간 원조·선후는 단정 불가"`
- 섹션 설명(길다): `"중앙선거관리위원회 후보자정보공개자료 기준입니다. ‘선관위 자료 제출’의 미등록은 자료를 성실히 제출했는지에 대한 참고 지표입니다."`
- 배지 라벨(짧다): `공약` / `목표` / `연속` / `기추진` / `재탕` / `적정` / `주의` / `부적정`

→ **짧은 라벨(배지)과 긴 설명(2~3줄)이 한 화면에 공존**한다. 위계를 분명히.

---

## 8. 디자인 자유 영역 (마음껏)

아래는 **전부 새로 제안 가능**하다. 기존 구현을 따르지 말 것:

- 색 팔레트·배경·표면 처리, 다크/라이트 여부
- 타이포(서체·스케일·위계) — 단 한국어 가독성 우선
- 레이아웃 패러다임(표 중심 / 카드 중심 / 분할 화면 / 매거진 / 대시보드 …)
- 정보 밀도(여백 많은 갤러리형 ↔ 데이터 빽빽한 분석형)
- "후보 N명 비교"를 푸는 방식(그리드/스크롤/스와이프/스택/매트릭스 …)
- 데이터 시각화(점수·납세율·집계를 막대/도넛/스파크라인/배지 등 무엇으로든)
- 모션·전환·마이크로인터랙션·진행감
- 일러스트/아이콘 체계(정보성 화면은 아이콘으로, 이모지는 §9 참고)
- 컴포넌트 형태(칩·배지·툴팁·모달·아코디언의 생김새)

**톤·컨셉도 시안마다 달라도 된다.** 발산을 권장한다.

---

## 9. 반드시 지킬 제약 (HARD RULES)

이것만은 모든 시안이 지켜야 한다.

1. **데이터 무결성**: 표시되는 라벨·수치·항목 구성은 바꾸지 않는다(디자인은 "표현"만, 의미·값은 그대로). 후보를 임의로 미화/비하하는 시각 처리 금지.
2. **중립성**: 특정 후보·정당을 시각적으로 유리/불리하게 만들지 않는다. 정당색은 **식별용**으로만(브랜드 강조 도구로 오용 금지). 후보 배치·강조는 공평하게.
3. **금지 안티패턴** *(사용자가 명시적으로 가장 싫어함)*: **"라운드 처리된 카드 + 한쪽 모서리에 굵은 색 보더"** 조합을 **어떤 카드 UI에도 쓰지 말 것.** 구분은 배경 틴트·여백·얇은 중립선 등으로.
4. **이모지 정책**: **공약 퀴즈 탭만** 게임성을 위해 이모지 허용. 그 외 정보성 화면(종합·공약비교·인물검증·모달·헤더)은 **장식용 픽토그램 이모지 금지** — 색·텍스트·여백·(필요시) 선/아이콘으로 위계 표현. (✓/✕ 같은 기능성 모노크롬 글리프는 무방.)
5. **접근성**: 상태(양호/주의/위험, 적정/주의/부적정)를 **색만으로 구분하지 말 것** — 반드시 텍스트/형태 병기. 본문 대비비 충분히.
6. **반응형**: 모바일~데스크톱 모두 동작. **후보 N명 가로 비교**가 모바일에서 깨지지 않게(스크롤·재배치·토글 등) 반드시 해법 제시.
7. **한국어**: 긴 한국어 문장의 줄바꿈·넘침·말줄임을 디자인에서 처리. 영문 전용 서체로 한글 가독성 해치지 말 것.
8. **등급/근거 분리**: 등급(중/적정 등 짧은 값)과 근거(긴 설명)는 시각적으로 분명히 구분(인라인 "중 — 긴 글" 식 금지).

---

## 10. 작업 지침 — 5가지 시안 (메인 화면 우선)

**1단계 — 메인 화면 5개 시안 발산**

- **대상 화면**: [§4.1 메인 화면](#41-탭1--종합-비교-overview--메인-화면) = **AppHeader + TabBar + 종합 비교(후보 요약 카드 + 핵심 정보 비교표)**. 첫 진입 인상을 좌우하는 화면이므로 여기부터.
- **수량**: **서로 뚜렷이 다른 5개 방향**. 살짝 변형한 5개가 아니라, **레이아웃 패러다임/정보밀도/톤** 중 한 축 이상에서 확연히 갈리게.
  - 예시 축(고를 필요 없음, 발산 참고용): 카드 중심 ↔ 표 중심 / 갤러리형 여백 ↔ 대시보드 밀도 / 저널리즘 정적 ↔ 인터랙티브 동적 / 후보 그리드 ↔ 후보 스택·스와이프.
- **각 시안 필수 산출**:
  1. 메인 화면 비주얼(대표 뷰 1장 이상). **데스크톱 + 모바일** 핵심 뷰를 함께 보이면 베스트.
  2. **컨셉 1줄 + 차별점 2~3줄**(이 시안의 정체성, 무엇이 다른가, 누구에게 잘 맞는가).
  3. 그 시안이 쓰는 핵심 토큰 단서(대표 색 1~2개·서체·radius·그림자 느낌) — 가볍게. *(정식·완결 토큰 시스템은 택일 후 [§11.1](#111-디자인-시스템--토큰--완벽하게-구축-필수-최우선)에서 완벽하게 구축한다.)*
- **공평성 체크**: 후보가 3명일 때와 5명일 때 둘 다 자연스러운지 한 시안에서라도 보여주면 좋다.

**2단계 — 사용자 택일**

- 사용자가 5개 중 **하나를 선택**한다(또는 둘을 합치라고 할 수 있음). 이 단계 전에는 나머지 화면으로 넘어가지 않는다.

---

## 11. 택일 후 고도화 지침

선택된 1개 방향으로 **앱 전체를 완성**한다. 이 단계의 **최우선·필수 산출물**은 [§11.1의 완결된 디자인 시스템과 토큰 세트]이다. 화면을 한 장씩 그리는 데서 끝내지 말고, **재현 가능한 시스템**으로 못 박아야 한다.

### 11.1 디자인 시스템 · 토큰 — **완벽하게 구축 (필수, 최우선)**

선택된 시안의 "분위기"를 감(感)이 아니라 **토큰**으로 고정한다. 아래를 **빠짐없이, 누락 없는 완결된 세트**로 구축할 것. (값 하나가 빠지면 구현에서 임의값이 끼어들어 시스템이 무너진다.)

**(a) 토큰 3계층 구조 (권장)**
- **원시(primitive/global)**: raw 값. 색 스케일(예: gray-50…900), 폰트, 단위, 기본 수치.
- **시맨틱(semantic/alias)**: 역할 기반 별칭. 예) `surface`, `surface-muted`, `text-primary/secondary/faint`, `border`, `accent`, `state-positive/warning/danger`(각 `-bg/-border/-fg`), `party-*`. **컴포넌트는 원시값이 아니라 시맨틱 토큰만 참조**하게 설계.
- **컴포넌트(component)**: 칩·배지·카드·툴팁·표 셀 등 컴포넌트 전용 토큰(필요한 경우에 한해).

**(b) 토큰 카테고리 — 전부 정의**
- **색**: 중립 스케일(배경/표면/표면-약/경계/텍스트 3단계 이상), 단일 액센트, **3색 상태(긍정·주의·부정)** 를 각각 **전경/테두리/배경틴트** 단계까지, 정당 식별색 + 그 위 텍스트의 **자동 대비 규칙**.
- **타이포**: 한글 서체 스택, 타입 스케일(디스플레이→제목→본문→캡션, 각 size/line-height/weight/letter-spacing), 숫자 정렬용 **mono** 스타일.
- **간격**: 베이스 단위 + 스페이싱 스케일(전 컴포넌트가 이 스케일만 사용).
- **형태**: radius 단계(sm/md/lg/pill 등), 보더 두께·색.
- **엘리베이션/그림자**: 레벨별(rest/hover/overlay 등).
- **모션**: duration 단계 + easing + 대표 트랜지션 프리셋(hover/모달/툴팁/페이지).
- **레이아웃 시스템**: 브레이크포인트, 콘텐츠 최대폭, 그리드/거터 규칙, z-index 레이어 스케일.
- (해당 시) **아이콘 규격**(사이즈·획 두께), **데이터 시각화 팔레트**(점수 막대·납세율 등에 쓸 색 매핑 — 후보별/상태별).

**(c) 모드**: 라이트가 기본. **다크 모드 지원 여부를 결정**하고, 지원한다면 **시맨틱 토큰 레벨에서 양쪽을 모두 정의**(원시 값만 스왑되도록). 미지원이면 그 결정을 명시.

**(d) 명명 규칙**: 일관된 토큰 네이밍 컨벤션을 정하고 문서화(예: `--color-surface`, `--color-state-danger-fg`, `--space-3`, `--radius-md`, `--shadow-overlay`, `--motion-fast`). 한 번 정한 규칙을 전 토큰에 일관 적용.

**(e) 산출 형식**: 구현이 **SCSS Module** 이므로 토큰은 **`:root`의 CSS custom properties + SCSS 변수/맵**으로 바로 떨어지게 제공. 추가로 디자인↔코드 동기화를 위해 **토큰 표(또는 JSON)** 형태로도 정리(토큰명·값·의미·사용처).

**(f) 인터랙션 상태 규칙**: `hover / active / focus(키보드) / disabled / selected / loading` 의 시각 변화를 **토큰 기반 규칙**으로 정의(예: hover=surface 한 단계 어둡게, active=`translateY(1px)`·scale, focus=토큰 색 외곽선). 컴포넌트마다 즉흥적으로 정하지 말 것.

**(g) 문서화·검증**: 토큰마다 **의미 1줄 + 사용 예**. 상태색·본문 텍스트의 **대비비(WCAG) 검증값** 포함. "이 토큰을 언제 쓰는가" 사용 가이드. 가능하면 **토큰 적용 예시(컴포넌트 스냅샷)** 로 시스템이 실제로 닫혀 있음을 증명.

> 목표: 이 시스템 문서만 보고도 새 컴포넌트나 새 선거구 화면을 **임의값 없이** 만들 수 있어야 한다. 그것이 "완벽한 구축"의 기준.

### 11.2 공용 컴포넌트 전수 디자인 (상태 포함)
[§5](#5-공용-컴포넌트-카탈로그-여러-화면-공유)의 전 컴포넌트: CandidateBadge / Status·Verdict 칩 / CompareGrid 셀 / SectionTitle / RecycleMiniBadges / 펼침-접힘 / 툴팁 — **§11.1 토큰만으로** 구성하고, 상태(기본·호버·활성·포커스·비활성·로딩·빈값)까지 명세.

### 11.3 나머지 화면 적용
공약 비교(통합 표 + 툴팁) · 인물·검증(검증 표 + 적정성 패널) · 공약 퀴즈(인트로→문항→결과→기록 흐름) · 후보 상세 모달 — 모두 동일 시스템으로.

### 11.4 상태·엣지 명세
빈 상태(공약 없음 셀, 평가 없는 선거구) · 긴 텍스트 · 음수(채무초과) · 후보 3↔5명 · 가로 스크롤 큰 표 · 모달 스크롤 · 모바일 전 화면.

### 11.5 반응형·접근성 명세
브레이크포인트별 레이아웃, 대비, 포커스 링, 키보드/터치 타깃.

### 11.6 (선택) 관리자 콘솔 톤 정합

**전달 형식 권장**: ① **디자인 시스템·토큰 세트(§11.1, 최우선)** ② 컴포넌트별 시안 + 상태/반응형 주석 ③ className(PascalCase) 제안. 이 셋이 갖춰지면 곧장 SCSS Module 구현으로 옮길 수 있다.

---

## 12. 부록 — 현재 토큰(참고용, 교체 가능)

지금 구현이 쓰는 값. **반드시 따를 필요 없음**. "어떤 결정들이 있었는지" 맥락으로만.

- 배경 `#f4f5f7` / 표면 `#ffffff` / 경계 `#e6e9ef`
- 본문 `#15171c`(오프블랙, 순흑 회피) · 보조 `#59616e` · 흐림 `#9aa1ad`
- 액센트 = 잉크 차콜 `#15171c` (AI 블루/퍼플 회피)
- 정당색(식별 전용): 민주 `#152484` · 국힘 `#e61e2b` · 개혁 `#ff7210` (그 외 정당색은 데이터에 동봉)
- 상태: 긍정 `#1f9d57` · 주의 `#c7860a` · 부정 `#d2403b`
- 서체: Pretendard(본문) / SF Mono·JetBrains Mono(숫자)
- 간격 8·12·16·24·32·48 / radius 10·14·20·pill / 그림자 = 넓고 옅은 잉크 디퓨전 / 모션 ease `cubic-bezier(0.16,1,0.3,1)`
- 콘텐츠 최대폭 1320px / 브레이크포인트 태블릿 900·모바일 600

---

*이 문서의 §1–§7은 "무엇을 담는가", §8–§9는 "자유와 한계", §10–§11은 "어떻게 진행하는가"이다. 5개 시안은 §10의 메인 화면부터 시작한다.*
